import 'package:flutter/material.dart';

class SearchHubScreen extends StatelessWidget {
  const SearchHubScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Search Hub (stub)'));
  }
}
